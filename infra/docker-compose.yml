services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: ccb
      POSTGRES_USER: ccb
      POSTGRES_PASSWORD: ccb
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ccb -d ccb"]
      interval: 5s
      timeout: 3s
      retries: 20

  api:
    build: ../backend
    env_file: ../backend/.env
    environment:
      DATABASE_URL: postgresql+psycopg2://ccb:ccb@db:5432/ccb
      ADMIN_EMAIL: admin@example.com
      ADMIN_PASSWORD: changeme
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8001:8000"  # Porta externa:interna (host:container)
    volumes:
      - ../backend:/app
    restart: unless-stopped

  web:
    build: ../frontend/app
    ports:
      - "3001:5173"  # Porta externa:interna (host:container)
    command: npm run dev -- --host
    environment:
      VITE_API_BASE_URL: http://localhost:8001  # Atualizado para nova porta da API
    volumes:
      - ../frontend/app:/app
      - /app/node_modules
      - ../casa.png:/app/public/casa.png:ro
    restart: unless-stopped

  api-test:
    build: ../backend
    env_file: ../backend/.env
    environment:
      DATABASE_URL: postgresql+psycopg2://ccb:ccb@db:5432/ccb
      ADMIN_EMAIL: admin@example.com
      ADMIN_PASSWORD: changeme
    depends_on:
      db:
        condition: service_healthy
    working_dir: /app
    command: sh -lc "pytest -q"
    volumes:
      - ../backend:/app

volumes:
  dbdata:
